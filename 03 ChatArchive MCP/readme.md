Description:
    Create an MCP that stores and retrives past chat messages from local stprage(JSON or SQlite).
    This project helps you learn about stateful MCP,Presistent storage and response management.

Example Tools:
    save_message(user, text)
    get_recent_messages(limit)
    
